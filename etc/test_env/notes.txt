Execution of a blink program generated by STM32 HAL:

main function:

  Call HAL_INIT():
    This function does the following -
    starts flash memory prefetch if available on the processor

    Call HAL_InitTick:
      
